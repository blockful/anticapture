# Generated / config artifacts that drown out the hand-written architecture in
# the knowledge graph. Respected by all native graphify operations (update,
# rebuild, post-commit/post-checkout hooks) via detect.py's .graphifyignore
# support — gitignore-compatible patterns. Replaces the old EXCLUDES that lived
# in .claude/hooks/graphify_rebuild.py.
**/generated/
**/drizzle/
infra/monitoring/grafana/
**/CHANGELOG.md
**/*.snapshot.json
# Machine-generated transport / tool outputs — noise in the architecture graph.
shared-memories/
graphify-out/
