{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["DATABASE_URL", "PORT"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [""]
    },
    "start": {},
    "install": {},
    "dev": {
      "persistent": true,
      "cache": false
    },
    "start:prod": {}
  }
}
