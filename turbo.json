{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["NODE_ENV", "DATABASE_URL"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**", ".next/cache/**"]
    },
    "start": {
      "cache": false,
      "persistent": true,
      "dependsOn": ["^build"]
    },
    "install": {},
    "dev": {
      "persistent": true,
      "cache": false
    },
    "start:prod": {
      "cache": false,
      "persistent": true,
      "dependsOn": ["^build"]
    }
  }
}
