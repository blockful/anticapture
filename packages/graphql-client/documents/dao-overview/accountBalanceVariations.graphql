query AccountBalanceVariations(
  $fromDate: String!
  $limit: PositiveInt = 10
) {
  accountBalanceVariations(fromDate: $fromDate, limit: $limit) {
    items {
      accountId
      previousBalance
      currentBalance
      absoluteChange
      percentageChange
    }
  }
}
