query TopVotingPowerVariations(
  $fromDate: String!
  $limit: PositiveInt = 10
) {
  topVotingPowerVariations(fromDate: $fromDate, limit: $limit) {
    items {
      absoluteChange
      accountId
      currentVotingPower
      percentageChange
      previousVotingPower
    }
  }
}
