query VotingPowerVariations(
  $fromDate: PositiveInt!
  $limit: PositiveInt = 10
) {
  votingPowerVariations(fromDate: $fromDate, limit: $limit) {
    items {
      absoluteChange
      accountId
      currentVotingPower
      percentageChange
      previousVotingPower
    }
  }
}
