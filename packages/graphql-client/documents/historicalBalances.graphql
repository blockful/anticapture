query getHistoricalBalances($addresses:  JSON!, $fromDate: String!) {
  accountBalanceVariations(addresses: $addresses, fromDate: $fromDate) {
    items {
      accountId
      previousBalance
    }
  }
}
