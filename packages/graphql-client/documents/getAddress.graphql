query GetAddress($address: String!) {
  getAddressEnrichment(address: $address) {
    address
    isContract
    arkham {
      entity
      entityType
      label
      twitter
    }
    ens {
      name
      avatar
      banner
    }
    createdAt
  }
}