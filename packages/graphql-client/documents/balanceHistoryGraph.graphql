query BalanceHistoryGraph(
  $accountId: String!
  $sortBy: queryInput_transfers_sortBy
  $sortOrder: queryInput_transfers_sortOrder
  $fromDate: Float
) {
  transfers(
    from: $accountId
    to: $accountId
    fromDate: $fromDate
    # conditional: queryInput_transfers_conditional
    sortBy: $sortBy
    sortOrder: $sortOrder
    limit: 1000
  ) {
    items {
      timestamp
      amount
      fromAccountId
      toAccountId
      transactionHash
      logIndex
    }
  }
}
