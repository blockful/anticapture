query GetDelegationHistoryItems(
  $delegator: String!
  $delegate: String
  $after: String
  $before: String
  $orderBy: String = "timestamp"
  $orderDirection: String = "desc"
  $limit: Int = 10
  $fromValue: BigInt = 0
  $toValue: BigInt
) {
  delegations(
    where: {
      delegatorAccountId: $delegator
      delegateAccountId: $delegate
      delegatedValue_gte: $fromValue
      delegatedValue_lte: $toValue
    }
    orderBy: $orderBy
    orderDirection: $orderDirection
    limit: $limit
    after: $after
    before: $before
  ) {
    items {
      delegateAccountId
      delegatedValue
      timestamp
      transactionHash
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
      startCursor
      endCursor
    }
  }
}
