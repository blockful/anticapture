query GetVotingPowerCounting ($address: String!, $limit: Int) {
  accountBalances(
    orderBy: "balance"
    orderDirection: "desc"
    limit: $limit
    where: {delegate: $address}
  ) {
    totalCount
  }
}