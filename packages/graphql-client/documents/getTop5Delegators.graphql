query GetTop5Delegators(
  $delegate: String!,
  $limit: Int = 6
) {
  accountBalances(
    where: { delegate: $delegate }
    orderBy: "balance"
    orderDirection: "desc"
    limit: $limit
  ) {
    items {
      accountId    
      balance
    }
  }
}