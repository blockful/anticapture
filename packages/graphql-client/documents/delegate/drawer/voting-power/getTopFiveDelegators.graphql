query GetTopFiveDelegators($delegates: [String], $limit: Int = 5) {
  accountBalances(
    delegates: $delegates
    fromValue: "0"
    orderDirection: desc
    limit: $limit
  ) {
    items {
      address
      balance
    }
  }
}
