query GetTopFiveDelegators($delegates: JSON, $limit: PositiveInt = 5) {
  accountBalances(
    delegates: $delegates
    fromValue: "0"
    orderDirection: desc
    limit: $limit
  ) {
    items {
      accountId
      balance
    }
  }
}
