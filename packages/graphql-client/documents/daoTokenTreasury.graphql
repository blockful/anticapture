query DaoTokenTreasury(
  $orderDirection: OrderDirection = asc
  $days: DaysWindow = _7d
) {
  getDaoTokenTreasury(orderDirection: $orderDirection, days: $days) {
    items {
      date
      value
    }
    totalCount
  }
}
