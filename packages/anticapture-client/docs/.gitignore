# Docusaurus build + cache
/.docusaurus
/build

# Generated OpenAPI MDX (produced by `docusaurus gen-api-docs`).
# Treated as a build artifact, like the package's Kubb `generated/` output.
/docs/api-reference
/docs/webhooks/reference

# Filtered OpenAPI spec (produced by `scripts/prepare-spec.mjs`).
/openapi

# Dependencies
/node_modules

# Misc
.DS_Store
*.log
