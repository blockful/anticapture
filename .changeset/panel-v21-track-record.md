---
"@anticapture/dashboard": minor
---

Add the "Track record" section to the panel: three outcome-framed case cards linking to the case write-ups, plus a testimonial carousel. `ClickableCard` now accepts an `href` so cards can render as links. Case URLs beyond ENS and the testimonial set are mocked pending published posts and content sign-off.
