---
"@anticapture/api": minor
"@anticapture/dashboard": minor
"@anticapture/client": patch
---

Add `GET /tokens/available` endpoint to fetch curated ERC-20 tokens for a DAO's chain from CoinGecko, and replace the raw contract address input in the Create Proposal transfer action with a token selector combobox.
