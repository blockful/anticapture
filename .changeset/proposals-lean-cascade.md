---
"@anticapture/gateful": patch
"@anticapture/api-gateway": patch
---

Regenerate API contracts to drop the lean proposal endpoints and pick up the
`lean` query param + optional execution-payload / body fields exposed by
`@anticapture/api`.
