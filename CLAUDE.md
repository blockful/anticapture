# Anticapture — agent instructions

The single source of truth for agent instructions in this repo is `AGENTS.md`,
shared by every agent (Claude Code, Codex, Cursor, ...). Do not add guidance
here — add it there, or to the per-package skills in `.claude/skills/`
(mirrored at `.agents/skills/`).

@AGENTS.md
