{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "infra/nodeful/Dockerfile",
    "watchPatterns": ["/infra/nodeful/**"]
  },
  "deploy": {
    "restartPolicyType": "ALWAYS"
  }
}
