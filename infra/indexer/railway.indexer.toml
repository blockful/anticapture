[build]
builder = "DOCKERFILE"
dockerfilePath = "./infra/indexer/Dockerfile.indexer"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
healthcheckPath = "/ready"
healthcheckTimeout = 172800
