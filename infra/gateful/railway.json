{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "infra/gateful/Dockerfile",
    "watchPatterns": [
      "apps/gateful/**",
      "infra/gateful/**",
      "apps/api/**/*.ts",
      "apps/address-enrichment/**/*.ts"
    ]
  },
  "deploy": {
    "healthcheckPath": "/health",
    "restartPolicyType": "ALWAYS",
    "restartPolicyMaxRetries": 3
  }
}
