/**
 * The uppercase mono label style (Figma `Alternative/xSmall`). It is a LABEL
 * style: row labels, param names, card titles. Content (hex, signatures,
 * URLs, sentences) is `Base/Regular`, i.e. plain `text-sm leading-5`.
 */
export const MONO_LABEL =
  "font-mono text-xs font-medium uppercase leading-4 tracking-wider";
