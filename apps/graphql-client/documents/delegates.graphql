query GetDelegates {
  accountPowers(orderBy: "votingPower", orderDirection: "desc", limit: 10) {
    items {
      votingPower
      account {
        type
        id
      }
      delegationsCount
    }
  }
}
