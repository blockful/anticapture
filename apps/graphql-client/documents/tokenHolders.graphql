query GetTopTokenHolders($after: String, $before: String, $limit: Int, $orderDirection: String ) {
  accountBalances(orderBy: "balance", orderDirection: $orderDirection, limit: $limit, after: $after, before: $before) {
    items {
      accountId
      balance
      delegate
      tokenId
      account {
        type
      }
    },
      pageInfo {
        endCursor
        hasNextPage
        hasPreviousPage
        startCursor
      }
  }
}
