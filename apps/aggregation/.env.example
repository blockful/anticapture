# Server Configuration
NODE_ENV=development
PORT=3000
HOST=0.0.0.0

# DAO API URLs - Add as many as needed using the DAO_API_* pattern
# The service will automatically detect all environment variables starting with DAO_API_
DAO_API_UNISWAP=https://uniswap-api.example.com
DAO_API_ENS=https://ens-api.example.com
DAO_API_OPTIMISM=https://optimism-api.example.com
# DAO_API_ARBITRUM=https://arbitrum-api.example.com
# DAO_API_COMPOUND=https://compound-api.example.com

# Request timeout in milliseconds
REQUEST_TIMEOUT=30000

# CORS - Use '*' for all origins or comma-separated list of origins
CORS_ORIGIN=*
